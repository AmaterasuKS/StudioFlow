# StudioFlow API secret configuration (example values only)
# Copy to .env.local (or set in OS environment), never commit real values.

Jwt__SecretKey=replace-with-strong-secret-min-32-chars
Jwt__Issuer=StudioFlow
Jwt__Audience=StudioFlow.Client
Jwt__ExpirationMinutes=60

ConnectionStrings__DefaultConnection=Data Source=studioflow.db
